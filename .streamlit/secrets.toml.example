# App OFICIAL (Streamlit Cloud, rama main)
contrasena_acceso = "1100"
# Compatibilidad con configuración anterior:
# codigo_acceso = "1100"

# App de PRUEBA (rama prueba): no pegue contrasena_acceso, o use:
# sin_contrasena_acceso = true

# La Matriz se sube sin contraseña (desbloqueada en Excel); no va aquí.
