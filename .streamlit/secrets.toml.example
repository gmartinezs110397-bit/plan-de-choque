# ── App OFICIAL (Streamlit Cloud, rama main) ───────────────────────────────────
contrasena_acceso = "1100"
# Compatibilidad con configuración anterior:
# codigo_acceso = "1100"

# ── App de PRUEBA (rama prueba) — elija UNA opción ─────────────────────────────
# Opción A (recomendada): no pegue nada de contrasena_acceso en Secrets; la app entra sola.
#
# Opción B: deje explícito que no pide contraseña de acceso:
# sin_contrasena_acceso = true
#
# La contraseña de la Matriz al consolidar se sigue pidiendo en pantalla (no va aquí).
